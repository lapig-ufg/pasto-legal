services:
  # ── Valkey/Redis — debouncing + session state ─────────────────────────
  valkey:
    image: valkey/valkey:8-alpine
    command: valkey-server --save "" --appendonly no
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - pasto-legal

  # ── FastAPI — WhatsApp webhook + pi RPC subprocess ────────────────────
  fastapi_app:
    build: .
    command: uv run uvicorn api.main:app --host 0.0.0.0 --port 3000 --reload
    ports:
      - "3000:3000"
    environment:
      - PYTHONPATH=/app
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - VALKEY_HOST=valkey
      - VALKEY_PORT=6379
      - VALKEY_DB=0
      - TOOL_BACKEND_URL=http://localhost:3000
      - GEMINI_API_KEY=${GOOGLE_API_KEY}
      - PI_PROVIDER=google
      - PI_MODEL=gemini-2.5-flash
    volumes:
      - .:/app
      - /app/.venv
      - ./tmp:/app/tmp
      - ./logs:/app/logs
      - ./pi-sessions:/tmp/pi-sessions
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env
    depends_on:
      valkey:
        condition: service_healthy
    networks:
      - pasto-legal

  # ── Streamlit — debug interface ───────────────────────────────────────
  streamlit_app:
    build: .
    command: uv run streamlit run ./api/interfaces/streamlit/streamlit_webapp.py --server.port 8080
    volumes:
      - .:/app
      - /app/.venv
      - ./tmp:/app/tmp
      - ./logs:/app/logs
    ports:
      - "8080:8080"
    environment:
      - PYTHONPATH=/app
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - FASTAPI_URL=http://fastapi_app:3000
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env
    depends_on:
      fastapi_app:
        condition: service_started
    networks:
      - pasto-legal

networks:
  pasto-legal:
    driver: bridge

volumes:
  pi-sessions:
